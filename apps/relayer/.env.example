# Same as contracts/.env after deploy + save-addresses
RPC_URL=https://ethereum-sepolia-rpc.publicnode.com
REGISTRY_ADDRESS=
FACTORY_ADDRESS=

# Relayer wallet (must be the same address set as relayer in TakeawayFactory)
RELAYER_PRIVATE_KEY=0x

# Optional: chain id (default 11155111 = Sepolia)
# CHAIN_ID=11155111

# Optional: ENS Registry (default mainnet); override if using ENS on another chain
# ENS_REGISTRY_ADDRESS=0x00000000000C2E074eC69A0dFb2997BA6C7d2e1e

# Optional: block to start indexing from (default 0)
# FROM_BLOCK=0
